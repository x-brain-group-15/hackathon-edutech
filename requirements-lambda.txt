# Requirements bundled into the Lambda deployment package.
#
# These are the ONLY packages Lambda needs at runtime.
# Dev-only packages (pytest, httpx) are NOT included — keeps zip size small.
#
# How this is used:
#   pip install -r requirements-lambda.txt -t ./package/
#   (see Makefile: make lambda-build)

# ---- Web framework ----
fastapi>=0.110
python-multipart>=0.0.9    # multipart/form-data for /upload
pydantic>=2.6

# ---- Lambda adapter ----
mangum>=0.17               # wraps FastAPI ASGI → Lambda event/response

# ---- AWS SDK ----
# boto3 is pre-installed in the Lambda Python 3.12 runtime.
# Include here to pin the version and avoid runtime drift.
boto3>=1.34

# ---- File parsing ----
pypdf>=4.0                 # PDF text extraction in handle_upload

# ---- Config ----
python-dotenv>=1.0         # loads .env if present (won't error if absent)
groq>=0.4.0 # Groq API client for fallback when Bedrock fails
