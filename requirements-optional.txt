# Optional dependencies — install only if you choose the matching backend.
#
#   pip install -r requirements-optional.txt
#
# pymongo:   required for USERSTORE_BACKEND=documentdb (also works for MongoDB Atlas)
# pymysql:   required for USERSTORE_BACKEND=mysql (RDS MySQL / Aurora MySQL)
# psycopg2:  required for USERSTORE_BACKEND=postgres (RDS PostgreSQL / Aurora PostgreSQL)
# mangum:    required if you deploy on AWS Lambda (wrap FastAPI app with Mangum)

pymongo>=4.6
pymysql>=1.1
psycopg2-binary>=2.9
mangum>=0.17
